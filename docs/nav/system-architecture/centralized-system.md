# 集中式系统

集中式系统是一种**将所有计算资源和数据存储集中在单一服务器或主机上的架构**。在这种系统中，所有的客户端请求都由中心服务器进行处理和响应。

**优点**：
- 管理和维护相对简单，便于统一控制和安全管理。
- 数据一致性高，易于实现集中备份和恢复。
- 部署成本较低，适合小型或对扩展性要求不高的场景。

**缺点**：
- 单点故障风险高，中心服务器出现故障会影响整个系统的可用性。
- 扩展性有限，难以应对大量并发请求或大规模数据处理需求。
- 性能瓶颈明显，服务器负载过高时可能导致响应变慢。