# CLAUDE.md

## Conventions

- 目录名称使用小写形式，多个单词使用"-"区分（仅对磁盘上的实际目录生效，config.mts 中 nav 和 sidebar 的 `text:` 标签保持原样）
- 文档简介直接在一级标题下方空一行用段落描述，不使用 `## 简介` 二级标题
- 提示信息统一使用 `> [!NOTE]` / `> [!TIP]` / `> [!WARNING]` 语法，不使用 `::: tip` / `:::` 语法
